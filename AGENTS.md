# AGENTS.md

このファイルは、このリポジトリで作業する Codex 向けの共通指示である。
案件固有の要件は `docs/tasks/` に置き、このファイルには追加しない。

## 指示の優先順位

1. ユーザーが明示した依頼・制約
2. 対象タスクの `docs/tasks/` 配下の要件書（存在する場合）
3. この `AGENTS.md`
4. `docs/` 配下の仕様・設計・運用文書
5. 既存実装から読み取れる慣例

指示同士、または指示と実装が矛盾する場合は、推測で判断せず、矛盾と影響範囲を示して確認する。特にセキュリティとプライバシーに関する規約は暗黙に上書きしない。

## プロジェクト概要

LINE から受信した不審者情報を住所解析・Geocoding して保存し、Google Maps 上に表示する Web アプリケーションである。

- `frontend/`: React 19、Vite、JavaScript、Google Maps
- `backend/`: Java 17、Spring Boot 3.5、Spring Data JPA
- データベース: PostgreSQL（実行環境）、H2（開発・テスト依存関係）
- 外部連携: LINE Messaging API、Google Geocoding API
- 主なデータフロー: LINE Webhook → メッセージ解析 → Geocoding → DB 保存 → REST API → 地図表示

詳細なドキュメントの整備方針と作成予定は [`docs/documentation-plan.md`](docs/documentation-plan.md) を参照する。作成予定の文書は、実際に存在することを確認してから参照する。

## 作業の基本方針

- 変更前に、対象コード、関連テスト、関連文書、設定ファイルを確認する。
- 依頼範囲に必要な最小限の変更を行い、無関係なリファクタリングを混ぜない。
- 実装と仕様文書が食い違う変更では、同じ変更内で関連文書も更新する。
- 現在の実装、決定済みの仕様、改善案を区別して記述する。
- 新しい依存関係、外部サービス、環境変数を追加した場合は、用途と設定方法を文書化する。
- API、Entity、DTO、画面表示の項目を変更する場合は、データフロー全体への影響を確認する。
- 不具合修正では、可能な限り再現テストまたは回帰テストを追加する。
- 未解決事項は `TODO` だけで終わらせず、理由または解消条件も記載する。

## コーディング規約

既存コードのスタイルを尊重しつつ、新規コードと変更箇所には次を適用する。

### 命名

- 定数は `UPPER_SNAKE_CASE` とする。
- 変数、引数、関数・メソッドは `lowerCamelCase` とする。
- クラス、インターフェース、React コンポーネントは `UpperCamelCase` とする。
- Java のパッケージ名は小文字とする。
- CSS クラス名は既存実装に合わせて kebab-case とする。
- 略語だけの名前や、意味の曖昧な短縮名を避け、役割が分かる名前を付ける。

JavaScript の `const` は「再代入しない変数」の宣言にも使用するため、すべてを定数名として扱わない。モジュールスコープの固定値や設定値など、概念上の定数は `UPPER_SNAKE_CASE`、関数内の計算結果・取得結果・オブジェクト参照は `lowerCamelCase` とする。

既存コードにはこの規約と異なる名前がある。対象機能の変更に必要でない限り、一括改名は行わない。

### Java / Spring Boot

- Controller は HTTP 入出力、Service は業務処理、Repository は永続化に責務を分ける。
- 外部 API 呼び出しとメッセージ解析は、単体テストで差し替え可能な境界を保つ。
- 入力値を検証し、例外を握りつぶさない。クライアント向け応答に内部例外や API キーなどの認証情報を露出しない。
- Entity をそのまま公開せず、API 境界では DTO を使用する。
- public なクラス・メソッドの意図が名前だけで明確でない場合は、実装内容ではなく目的・制約をコメントする。

### JavaScript / React

- 関数コンポーネントと Hooks を使用する。
- 副作用は `useEffect` などの適切な境界に置き、描画中に外部状態を変更しない。
- API の読み込み中、失敗、空データの状態を考慮する。
- API URL やキーなど環境依存の値をソースへ直書きしない。
- 整形は `frontend/.prettierrc`、静的解析は `frontend/eslint.config.js` に従う。

より詳細な言語別規約が必要になった場合は `docs/development/coding-guidelines.md` を正本として作成し、この節には要点とリンクだけを残す。

## セキュリティとプライバシー

このプロジェクトでは LINE ユーザー ID、住所、座標、不審者情報を機微なデータとして扱う。

- この文書でいう認証情報は、API キー、LINE のチャネルシークレット・アクセストークン、パスワード、秘密鍵、その他の認証に用いる値を指す。認証情報の実値をリポジトリ管理対象にせず、ステージングまたはコミットしない。
- LINE Webhook は、保存処理の前に署名を検証し、検証に失敗したリクエストを処理しない。
- リクエスト本文、署名、ユーザー ID、完全な住所、認証情報の実値をログへ出力しない。API キーを含み得る完全な Geocoding リクエスト URL も出力しない。
- 公開用 REST API のレスポンスに、LINE ユーザー ID など画面表示に不要な個人情報を含めない。
- CORS の許可元を理由なく広げない。ワイルドカードと資格情報の併用を避ける。
- セキュリティ上の問題を発見した場合は、影響と暫定状態を明示し、無関係な変更へ埋もれさせない。

## 検証

変更範囲に応じて、以下をリポジトリルートから実行する。

### フロントエンド

依存関係を初期化する場合:

```bash
cd frontend
npm ci
```

変更を検証する場合:

```bash
cd frontend
npm run lint
npm run build
```

### バックエンド

```bash
cd backend
./mvnw test
```

### 完了時

- 実行した検証と結果を報告する。
- 実行できなかった検証は、省略せず理由を報告する。
- 自動テストがない UI・地図変更は、確認した画面状態と未確認事項を報告する。
- 差分を確認し、セキュリティおよび Git 操作の規約に反するファイルや、無関係な変更が含まれていないことを確認する。

## ドキュメント

- 詳細な仕様、設計、開発手順、セキュリティ方針、テスト方針は `docs/` に置く。
- 案件固有の背景、要件、対象外、受け入れ条件は `docs/tasks/` に置く。
- 同じ説明を複数文書へ複製せず、正本へ相対リンクする。
- 文書内のコマンド、パス、設定名は実装と照合する。
- 設定例には、設定名と明らかなダミー値のみを使用する。

## Git 操作

- ユーザーの既存変更を上書き・破棄しない。
- `git status` と差分を確認し、担当した変更だけを扱う。
- ユーザーの明示的な依頼なしに、コミット、push、rebase、reset、ブランチ作成を行わない。
- 管理対象外のビルド・テスト出力、一時ファイル、ローカル専用設定をステージングしない。リポジトリで管理する生成ファイルは既存方針に従う。
- 大きな変更は、独立して確認できる論理的な作業単位に分け、差分と検証結果を単位ごとに整理する。
- コミットを依頼された場合は、原則として 1 つの目的につき 1 コミットとする。
